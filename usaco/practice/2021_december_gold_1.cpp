#include <bits/stdc++.h>
using namespace std;

int k;
void solve(vector<int>& a){
	int n = a.size();
	vector<long long> dp0(n+1), dp1(n+1);

}

int main(){
	cin.tie(0)->ios_base::sync_with_stdio(false);

	int t, n; cin >> t >> n >> k;
}
